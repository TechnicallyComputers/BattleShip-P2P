# Known Bugs & Resolved Issues

This directory documents significant bugs encountered during the port, their symptoms, root causes, and fixes. When fixing a new bug, add an entry here so future sessions can recognize the same class of issue.

## Index

| Date | Slug | Summary |
|------|------|---------|
| 2026-04-26 | [intro_explosion_overlay_layering](intro_explosion_overlay_layering_2026-04-26.md) | **RESOLVED** — mvOpeningRoom desk→arena transition explosion (tics 1040–1080) had two visual bugs: (1) opaque white center where the red/white silhouette should be, and (2) effect rendered behind all scene geometry, visible *through* the desk-room window in the wallpaper. Both rooted in libultraship's Fast3D not emulating the N64 color-image-redirect-to-Z-buffer idiom. The Overlay's white-PRIM mesh, intended to write a Z mask, instead painted directly onto the framebuffer (white blob). And the transition camera's `dl_link_priority=95` made it draw earliest (= bottom of stack) — on N64 this didn't matter because the redirect handled layering via depth manipulation, not draw order. Fix in two PORT-gated hunks in `mvopeningroom.c`: early-return from the Overlay display callback (the mesh contributes nothing visible without redirect emulation), and lower the transition camera's `dl_link_priority` 95 → 30 so it draws after fighter=40 / main=80 / wallpaper=90. Supersedes the earlier partial libultraship fixes documented in `docs/intro_explosion_alpha_cutout_handoff_2026-04-25.md`; remove both PORT branches if approach #4 (real redirect emulation) ever ships |
| 2026-04-25 | [primdepth_unimplemented](primdepth_unimplemented_2026-04-25.md) | **RESOLVED** — `gDPSetPrimDepth` was a no-op stub in libultraship's interpreter (latent since the original Emill Fast3D engine; upstream Kenix3 still carries the same TODO). Game sprites that set `gDPSetDepthSource(G_ZS_PRIM)` ended up at clip-space z=0 (front plane) instead of their intended depth — port-wide layering fail. Most visible: 2D portrait card drew in front of the 3D fighter model in description scenes. Found while debugging the mvOpeningRoom transition explosion (wallpaper at `gDPSetPrimDepth(36863, 1)` was occluding the explosion tris). Fix in 3 hunks: store `prim_depth_z`/`prim_depth_dz` in the RDP struct, parse in `gfx_set_prim_depth_handler_rdp`, override per-vertex Z in `GfxSpTri1` when `other_mode_l & G_ZS_PRIM`. Linear `primDepth/65535` mapping into clip space — sufficient for sprite layering. The explosion itself is still hidden — separate alpha-cutout-coverage gap, see `docs/intro_explosion_alpha_cutout_handoff_2026-04-25.md` |
| 2026-04-25 | [spline_interp_block_halfswap](spline_interp_block_halfswap_2026-04-25.md) | **RESOLVED** — Samus's dodge / morph-ball roll played the animation but didn't translate her position. `SetTranslateInterp` events in fighter figatrees point to a `SYInterpDesc` plus three full-width data tables (`Vec3f points`, `f32 keyframes`, `f32 quartics`). The figatree halfswap pass swaps u16 halves of every non-token u32 in the file — correct for AObjEvent16 streams, wrong for f32/Vec3f data. `syInterpCubic` then read garbage control points (translate.x stuck at `9.6e9`), `out` never changed across frames, `vel_ground = (translate.now - translate.prev) * scale = 0`. Fix un-halfswaps each data block on first access in `syInterpGetPoints` / `Keyframes` / `Quartics`, gated on `port_aobj_is_in_halfswapped_range`. Header left alone — its halfswap happens to land `points_num=5` on correctly-decoded bytes by coincidence; un-halfswapping the header crashed the spline math. Same family as `aobjevent32_halfswap_2026-04-18` |
| 2026-04-25 | [whispy_canopy_metal_fallback_texture](whispy_canopy_metal_fallback_texture_2026-04-25.md) | **RESOLVED** — Pupupu Whispy canopy stripe / matanim feedback artifact on macOS Metal, present since VS mode first booted. CC modes that declare TEXEL1 without a matching `LoadBlock` for tile 1 left `mCurrentTextureIds[1]` at its zero-init default; in libultraship's Metal backend that index belongs to the screen drawable (`mTextures[0]`), so the encoder bound the live color buffer at sampler slot 1 and the fragment shader sampled the in-progress screen — `mix(curr, screenfb, frac)` produced the stripe. OpenGL's GLD driver substitutes a zero texture for unbound samplers (the `GLD_TEXTURE_INDEX_2D unloadable` warning called out in `port/port.cpp`); Metal-cpp does no such substitution. Fix lives in libultraship: allocate a 1×1 transparent-black RGBA texture in `gfx_metal.cpp::Init()`, point `mCurrentTextureIds[]` at it by default, and redirect the bind in `DrawTriangles` whenever the resolved slot would alias `mTextures[0]` or has null tex/sampler. Supersedes the `SSB64_CURR_EQ_NEXT=1` / `port/port.cpp` workaround |
| 2026-04-24 | [audio_mixer_macro_clobber](audio_mixer_macro_clobber_2026-04-24.md) | **RESOLVED — BGM audible.** Final unblock after the bank-swap and ALParam-variant fixes. `include/PR/abi.h` auto-included `port/audio/mixer.h` at its tail, but `n_abi.h` (defines `n_aLoadBuffer`/`n_aADPCMdec`/etc.) was included LATER via `n_synthInternals.h`. Result: mixer.h's `#undef + redefine` of the n_a* family ran while those macros didn't yet exist (no-op `#undef`s, redefines applied), then n_abi.h silently overrode mixer.h's redefinitions back to the original Acmd-write macros. Pull chain wrote Acmd words that nothing consumed → silent BGM. The standard `aXxx` family was unaffected (defined before mixer.h ran, never redefined). Fix: removed the auto-include from abi.h; n_env.c already includes mixer.h after the n_audio headers. Verified: `first non-zero sample detected` fires; BGM 33 plays |
| 2026-04-24 | [audio_alparam_variant_overrun](audio_alparam_variant_overrun_2026-04-24.md) | **RESOLVED** — Immediately after the bank-swap fix, BGM 33 crashed in `n_alEnvmixerPull+100` with `fault_addr=0x7f0d`. Root cause: `__n_allocParam()` returns 40-byte ALParam slots, but `ALStartParamAlt` grew to 48 bytes on LP64 (its `wave *` widened 4→8 plus a 4-byte alignment pad pushed `unk1C/unk1D` to offsets 40/41). `n_alSynStartVoiceParams_Alt`'s writes overran the freelist's adjacent param's `next` pointer; the byte pattern `unk1C=0x05, unk1D=0x7F` produced exactly the corrupt em_ctrlList=0x0000000000007F05 we saw. Fix pads ALParam to 48 bytes under PORT and adds `_Static_assert(sizeof(variant) <= sizeof(ALParam))` for ALStartParamAlt/ALStartParam/ALFreeParam |
| 2026-04-24 | [audio_bridge_bank_swap](audio_bridge_bank_swap_2026-04-24.md) | **Fix shipped; awaiting in-game audible verification.** `audio_bridge.cpp` loaded `sounds2_ctl` into `sSYAudioSequenceBank2` (music CSP slot) and `sounds1_ctl` into `sSYAudioSequenceBank1` (SFX direct-index slot) — the original game's `SYAudioSettings` does the opposite (`bank2_start = sounds1_ctl`, `bank1_start = sounds2_ctl`). The decomp's `// load sfx bank` comment above the `sSYAudioSequenceBank2` assignment misled the bridge author; downstream `n_alCSPSetBank(player, sSYAudioSequenceBank2)` makes that slot the music bank. Result: music CSP got the 322-sound SFX bank with degenerate keymaps, every NoteOn missed in `__n_lookupSoundQuick` → silent BGM. Fix swaps the two parser blocks |
| 2026-04-24 | [audio_cseqplayer_altbank_lp64](audio_cseqplayer_altbank_lp64_2026-04-24.md) | **Fix shipped; audio still silent for unrelated reason.** `N_ALCSPlayer` / `N_ALSeqPlayer` store two extra bank slots as `s32 unknown0/unknown1` right after `ALBank *bank`, and MIDI CC22 picks between them via `*(&seqp->bank + idx)`. On LP64 the pointer is 8 bytes but the s32s are 4, so the pointer-array idiom reads garbage after the first slot. Also `(s32)(intptr_t)` truncates bank pointers on write. Widened both to `ALBank *` under PORT and dropped the truncating casts. BGM 33 doesn't exercise alt-banks, so the change is a latent-bug fix — synth is still producing zeros because of a separate music-bank keymap lookup issue (handoff `docs/audio_buildout_2026-04-24.md`) |
| 2026-04-24 | [training_mode_sprite_array_stride](training_mode_sprite_array_stride_2026-04-24.md) | **RESOLVED** — Training Mode entry LP64 pointer-array stride sweep: three successive crashes in the same scene-start path (`fault_addr=0x1fe00140024` then `0x20200000219` etc., all adjacent-token-as-u64 fingerprints). Affected: `SC1PTrainingModeSprites::sprite` (8B→16B struct stride), `SC1PTrainingModeMenu::display_option_sprites` (`Sprite**` → 4B→8B slot stride), `menu_option_sprites` (same). All pinned to `u32`/`u32*` under PORT with `PORT_RESOLVE` at readers and `portFixupStructU16` on the Vec2h pos word. Same class as bonus-stage `anim_joints` and mball `file_head` |
| 2026-04-24 | [training_mode_wallpaper_token_truncation](training_mode_wallpaper_token_truncation_2026-04-24.md) | **RESOLVED** — Training Mode entry SIGSEGV `fault_addr=0x34` inside `lbCommonMakeSObjForGObj` via `grWallpaperMakeStatic`. `MPGroundData::wallpaper` is `Sprite*` on N64 but `u32` token on the port. `sc1PTrainingModeLoadWallpaper` wrote the raw host pointer — the only post-load writer of this field — truncated to 32 bits on LP64, and `grWallpaperMakeStatic`'s `PORT_RESOLVE` returned NULL for the bogus "token." Fix: wrap the write in `PORT_REGISTER(sprite)` so it round-trips through the pointer table |
| 2026-04-24 | [oscontpad_lus_sizeof_overrun](oscontpad_lus_sizeof_overrun_2026-04-24.md) | **RESOLVED** — Menu-only input double/triple-fire (pause toggles, CSS puck grab-then-drop, CPU level jumps 2–3). `OSContPad` has two layouts in the same build: decomp 6-byte (game C) vs libultraship 0x24-byte (C++ stub). `libultraship/.../os.cpp osContGetReadData` used `sizeof(OSContPad)` (LUS) and memset'd 144 bytes into a 24-byte caller buffer, zeroing the leading fields of the adjacent `sSYControllerDescs[]` every Read. `unk00` (prior-frame button state for edge detection) went to 0 each frame → held button re-fired `button_tap` every tick. Fix marshals through a local LUS-layout stack buffer and writes only the 4 decomp fields into the caller's pointer. `button_hold`-driven gameplay inputs were unaffected because `unk00` gets restamped with `button` a few lines after the clobber |
| 2026-04-24 | [itattributes_rom_layout_rewrite](itattributes_rom_layout_rewrite_2026-04-24.md) | **RESOLVED** — No thrown item (Pokeball, Capsule, Egg, Barrel, Link's Bomb, GShell) ever connected with a fighter mid-flight. Root cause: port `ITAttributes` sizeof was `0x50`, true ROM stride is `0x48`; artificial `u32 : 11/16/14` pads in the LE branch shifted every field from `0x1E` onward by 2 bytes and pushed `attack_count` onto pad bits (read 0 → hit loop never ran). `type` and `hitstatus` also declared in a nonexistent final u32 — they live in the same u32 as `priority` / `kb_base` / `can_*` at `0x3C`. Rewrote struct against ground truth from disassembling `itManagerMakeItem` at `0x8016E174`. Supersedes `itattributes_type_field_offset` and `item_map_coll_bottom_sign` workarounds |
| 2026-04-22 | [loadtile_fixup_last_row_right_edge](loadtile_fixup_last_row_right_edge_2026-04-22.md) | **RESOLVED** — Yoster 5 thin horizontal semi-opaque stripes. `GfxDpLoadTile` byte-order fixup used `start_offset_bytes + orig_size_bytes` (tile stride × tile_h) as its upper bound, but the decoder reads at full-image stride. For any LoadTile where `tile_width < full_image_width`, the last row's rightmost `(full_w - tile_w)` pixels stayed in pass1-BSWAP32 (LE) state. Byte-reversed RGBA puts the background's `F7` pink-channel byte into the alpha slot, drawing an opaque strip at the bottom of every sub-tile load. Fix: `tile_height * full_image_line_size_bytes` as the fixup extent |
| 2026-04-22 | [mpgrounddata_layer_mask_u8_byteswap](mpgrounddata_layer_mask_u8_byteswap_2026-04-22.md) | **RESOLVED** — Sector Z Great Fox hull missing (and related partial-render reports on other stages). `MPGroundData.layer_mask` is a `u8` at struct offset 68; pass-1 blanket BSWAP32 pushes the real value to offset 71, so the native `u8` read picks up the zero padding byte. `grDisplayMakeGeometryLayer` then always takes the pri_proc_display branch (`gcDrawDObjTreeForGObj`) instead of sec_proc_display (`gcDrawDObjTreeDLLinksForGObj`), which walks DObjDLLink chains as raw Gfx*. Nine sequential `{list_id, dl_token}` pairs decode as G_NOOP runs with token payloads (fingerprint: `bytes=00000000 00000131 00000004 00000000` at d=4 after a d=3 G_DL). Fix adds `portFixupStructU32` helper to un-swap the layer_mask u32 word at stage init. Also resurfaces the class: any u8 in a loaded struct controlling behaviour needs the same audit |
| 2026-04-22 | [fighter_appearr_body_frozen](fighter_appearr_body_frozen_2026-04-22.md) | **RESOLVED** — Mario / Link Appear intros rendered bodies frozen in Wait stance while entry effects (pipe, beam, capsule) animated correctly. Regression from 3161c13's walker hardening: the new "raw form also parses → reject" rule triggered on one-step End false positives (halfswap-corrupted u32s whose bits [31:25] happen to equal 0), causing the walker to reject halfswap-corrupted streams as "ambiguous native." Parser read the raw bytes, hit End immediately, terminated every body joint's animation on the first parse call. Fix: replace binary ambiguity rule with length comparison — raw must walk ≥ 2 commands to count as genuinely native, otherwise un-halfswap wins |
| 2026-04-20 | [particle_banks_silent_empty](particle_banks_silent_empty_2026-04-20.md) | **RESOLVED** — Every in-game particle effect (hit sparks, fire-flower fire, explosion flame, Samus fire, Mario coins, fireball sparkles, stage hazards, title/attract particles) silently failed to render. The 2026-04-08 DMA-segfault fix papered over the crash by registering a dummy bank without calling `lbParticleSetupBankID`, so every `lbParticleMakeChildScriptID` bounds check (`script_id >= sLBParticleScriptBanksNum[id]`) failed against `scripts_num == 0` and returned NULL silently. Fix: new `port/bridge/particle_bank_bridge.cpp` loads all 9 banks from `particles/*` in ssb64.o2r, byte-swaps the headers (bytecode is left alone — `lbParticleReadFloatBigEnd`/`ReadUShort` are already LE-safe), calls `lbParticleSetupBankID`. Dedup via pristine-path cache + scripts_lo-keyed working-copy eviction. Supersedes `particle_bank_dma_2026-04-08.md` |
| 2026-04-20 | [color_image_to_zbuffer_draws](color_image_to_zbuffer_draws_2026-04-20.md) | **RESOLVED (TextureRectangle only)** — Off-screen fighter magnifier bubble rendered as an opaque black square with the fighter icon inside. SSB64 uses an N64 trick that redirects the color image to the Z buffer to build an alpha-masked stencil before the visible XLU draw. Fast3D doesn't emulate address redirection — all draws go to the primary FB — so the mask draw landed as a visible opaque rectangle. Fix adds the `color_image == z_buf_address` skip (already present in `GfxDpFillRectangle`) to `GfxDpTextureRectangle`. `GfxSpTri1` still needs the same guard if `mvOpeningRoom` transitions ever regress |
| 2026-04-20 | [sprite_decode_stride_mismatch](sprite_decode_stride_mismatch_2026-04-20.md) | **RESOLVED** — Tutorial banner / textbox / HereText arrow / fighter-select puck / 1P character portraits / 1P map wallpaper all rendered with diagonal shear. `ImportTextureRgba16` / `ImportTextureRgba32` / `ImportTextureCi8` reassigned `fullImageLineSizeBytes` from the *clamped* `width` when SetTileSize made `tex_width < width_img`, so the decode read every subsequent row from the wrong offset. Generalizes the 2026-04-14 IA8/I4 ClampUploadWidthToTile fix; CI8 also had a stacked decode-before-clamp bug that required restructuring |
| 2026-04-20 | [sprite_32bpp_tmem_swizzle](sprite_32bpp_tmem_swizzle_2026-04-20.md) | **RESOLVED** — RGBA32 sprites (SMASH logo, fighter portraits, any 32bpp) rendered with scanline shear because the TMEM-line-swizzle fix from 2026-04-10 hardcoded the 4b/8b/16b swap granularity (XOR-4) and skipped 32bpp entirely. Real fix: for 32bpp, swap 8-byte halves of 16-byte groups (XOR-16 at DRAM level) because `G_IM_SIZ_32b_LOAD_BLOCK = G_IM_SIZ_32b` puts each texel in two TMEM words via bank split |
| 2026-04-20 | [wpattributes_bitfield_padding](wpattributes_bitfield_padding_2026-04-20.md) | **RESOLVED** — Mario 64% damage / Fox 0 damage / Samus pass-through all one bug: port's `WPAttributes` bitfield positions didn't match IDO BE's physical layout. IDO packs `angle:10` into the u16 pad at 0x26, and `knockback_weight` belongs to Word 1 not Word 2. Verified by disassembling IDO-compiled reader. See [docs/debug_ido_bitfield_layout.md](../debug_ido_bitfield_layout.md) for the audit method |
| 2026-04-20 | [itattributes_type_field_offset](itattributes_type_field_offset_2026-04-20.md) | **SUPERSEDED** by 2026-04-24 itattributes_rom_layout_rewrite: the surgical `type`-override workaround was removed when the full struct got rewritten; `attr->type` now reads natively |
| 2026-04-20 | [item_map_coll_bottom_sign](item_map_coll_bottom_sign_2026-04-20.md) | **SUPERSEDED** by 2026-04-24 itattributes_rom_layout_rewrite: the "ROM stores as positive magnitude" observation was wrong — the old struct was reading `map_coll_width`'s positive bytes from the wrong offset; the real `map_coll_bottom` at `0x2E` is already signed-negative in ROM. Workaround's negation removed |
| 2026-04-20 | [item_arrow_gobj_implicit_int](item_arrow_gobj_implicit_int_2026-04-20.md) | Dropped-item despawn segfault: `ifCommonItemArrowMakeInterface` called without prototype → implicit-int rule truncates the 64-bit `GObj*` return to 32 bits on LP64 |
| 2026-04-20 | [mnplayers1pgame_timegobj_stale](mnplayers1pgame_timegobj_stale_2026-04-20.md) | 1P character-select crash on re-entry from VS: `sMNPlayers1PGameTimeGObj` static slot was missing from `mnPlayers1PGameInitVars`'s reset list, so its stale value survived the per-scene heap reset and `gcEjectGObj` then dereferenced freed memory |
| 2026-04-19 | [rumble_event_bitfield_init](rumble_event_bitfield_init_2026-04-19.md) | Battle-start hang: positional initializers on endian-conditional bitfield stored opcode as param; gmRumbleUpdateEventExecute spins on phantom End |
| 2026-04-18 | [aobjevent32_halfswap](aobjevent32_halfswap_2026-04-18.md) | Fighter figatree u16-halfswap corrupts AObjEvent32 command bitfields; lazy per-stream un-halfswap walker at EVENT32 reader entry |
| 2026-04-14 | [title_border_right_edge_slice](title_border_right_edge_slice_2026-04-14.md) | ce89700's zero-fill of sprite trailing columns dimmed the right edge of the title-screen border sprite; fix replicates edge pixel instead |
| 2026-04-13 | [samus_charge_shot_hit_detection](samus_charge_shot_hit_detection_2026-04-13.md) | **SUPERSEDED** by the 04-20 `WPAttributes` bitfield fix: `attack_count` was being decoded from the wrong bits (read 0 instead of the true 1), so the hit loop never iterated. The bypass-path theory was a red herring |
| 2026-04-11 | [mpvertex_byte_swap](mpvertex_byte_swap_2026-04-11.md) | MPVertexData / MPVertexIDs byte-swap deferral broke Jungle floor collision |
| 2026-04-11 | [controller_motorevt_lp64](controller_motorevt_lp64_2026-04-11.md) | Unk80045268 ContMotorEvt struct pun faults on LP64 |
| 2026-04-11 | [fighter_tile_masks_wrap](fighter_tile_masks_wrap_2026-04-11.md) | Missing SetTile masks/maskt clamp → black squares on fighter body parts |
| 2026-04-11 | [loadtlut_cross_boundary](loadtlut_cross_boundary_2026-04-11.md) | LOADTLUT spillover across 256-byte palette boundary dropped entries |
| 2026-04-11 | [osmesg_union](osmesg_union_2026-04-11.md) | OSMesg `void*` vs union type split produced uninitialized upper bytes |
| 2026-04-11 | [bzero_recursion_arm64](bzero_recursion_arm64_2026-04-11.md) | Stubbed bzero on macOS/arm64 recursed infinitely via memset lowering |
| 2026-04-10 | [sprite_texel_tmem_swizzle](sprite_texel_tmem_swizzle_2026-04-10.md) | Sprite byteswap deferral + N64 RDP TMEM line swizzle left sprites sheared |
| 2026-04-08 | [segment_0e_gdl](segment_0e_gdl_2026-04-08.md) | Segment 0x0E G_DL resolution exception branched into the graphics heap |
| 2026-04-08 | [particle_bank_dma](particle_bank_dma_2026-04-08.md) | **SUPERSEDED** by 2026-04-20 particle_banks_silent_empty: avoided the segfault but left every bank empty, silently breaking all particle effects |
| 2026-04-08 | [ground_geometry_collision](ground_geometry_collision_2026-04-08.md) | PORT_RESOLVE + all-u16 collision struct byte-swap fixes |
| 2026-04-07 | [dl_normalization_guard](dl_normalization_guard_2026-04-07.md) | Heuristic guard mis-detected packed DLs starting with w1=0 sync commands |
| 2026-04-06 | [display_list_widening](display_list_widening_2026-04-06.md) | Packed 8-byte resource DLs needed widening to 16-byte native format |
